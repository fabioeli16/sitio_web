//SLIDER TESTIMONIOS---------------------------------------------------------------
/*
const anterior=document.querySelector(".contenedorAnterior");
const sig=document.querySelector(".contenedorSiguiente");


const slider1 =document.querySelector(".contenedorSlider");
const slider2 =document.querySelector(".contenedorCartaSlider");
const elementos=2;


anterior.addEventListener("click",()=>{
    slider1.scrollLeft -= slider2.offsetWidth/2;
})

sig.addEventListener("click",()=>{
    slider1.scrollLeft += slider2.offsetWidth/elementos;
})


window.addEventListener('resize', function(event) {
    slider1.scrollLeft = 0;
}, true);
*/